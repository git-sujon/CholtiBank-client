"use client"

const WithdrawMoneyFormPage = ({ params }: { params: { source: string } }) => {

    const source = params.source[0]

    return (
        <div>
            WithdrawMoneyFormPage
        </div>
    );
};

export default WithdrawMoneyFormPage;